# Styles

This folder contains additional style files beyond the global styles.

## Structure
- CSS modules for component-specific styles
- Shared style utilities
- Theme configuration

## Example
```
styles/
├── variables.css       # CSS custom properties
└── components/         # Component-specific styles
    └── Button.module.css
```

Note: Global styles are in `src/app/globals.css`